export default function LoginPage() {
  return (
    <section className="card">
      <h1>Login</h1>
      <p>Firebase Auth is the production login layer for Cloud Family Mode. Configure Firebase credentials to enable live account sessions.</p>
      <a className="btn secondary" href="/demo">Use local demo instead</a>
    </section>
  );
}
