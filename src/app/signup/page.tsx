export default function SignupPage() {
  return (
    <section className="card">
      <h1>Signup</h1>
      <p>Cloud Family Mode signup is backed by Firebase Auth, family bootstrap, child profile setup, and parent privacy settings. Configure Firebase credentials to enable live account creation.</p>
      <a className="btn" href="/onboarding">Preview onboarding</a>
    </section>
  );
}
